#include <stdio.h>
int main()
{
    int n,i,j,k;
    char string[16];
    while(scanf("%d",&n)==1)
    {
        if (n==0)
        {
            break;
        }
        else
        {
            for (i=0; i<n; i++)
            {
                scanf(" %[^\n]",string);
                if (string)
            }
        }
    }
    return 0;
}
