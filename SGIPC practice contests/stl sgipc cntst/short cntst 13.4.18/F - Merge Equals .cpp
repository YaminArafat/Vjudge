#include <bits/stdc++.h>
using namespace std;
bool flag[1000000000];
int main()
{

}
/*
long long n,i,x,z=0,j,cnt=0,f,temp=100000000000000,one,two;
    scanf("%lld",&n);
    //long long arr[n];
    vector<long long>arr;
    for(i=0; i<n; i++)
    {
        scanf("%lld",&x);
        /*if(!flag[x])
        {
            cnt++;
            flag[x]=1;
        }
        arr.push_back(x);
    }
    /*for(i=0;i<arr.size()-1;i++)
    {
        for(j=i+1;j<arr.size();j++)
        {
            if(arr[i]==arr[j]&&arr[i]<temp)
            {

            }
        }
    }
    while(1)
    {
        temp=100000000000000,z=0,cnt=1;
        for(i=0; i<arr.size()-1; i++)
        {
            f=1;
            for(j=i+1; j<arr.size(); j++)
            {
                if(arr[i]==arr[j] && arr[i]<temp)
                {
                    //cnt++;
                    one=i;
                    //z=1;
                    if(f)
                    {
                        two=j;
                        f=0;
                    }
                    temp=arr[i];
                    break;
                }
            }
        }
        /*if(!z)
        {
            break;
        }
        //cout<<one<<" "<<two<<endl;
        arr[two]=temp*2;
        arr.erase(arr.begin()+one);
        long long mx=0;
        //for(i=0; i<arr.size()-1; i++)
        {
            //printf("%lld ",arr[i]);
            for(j=1; j<arr.size(); j++)
            {
                if(arr[0]!=arr[j] && !flag[arr[j]])
                {
                    cnt++;
                    flag[arr[j]]=1;
                }
            }
            if(cnt>mx)
            {
                mx=cnt;
            }
        }
        //}
        if(cnt==arr.size())
        {
            break;
        }
        //cout<<endl;
    }
    printf("%d\n",arr.size());
    for(i=0; i<arr.size(); i++)
    {
        printf("%lld ",arr[i]);
    }
    return 0;
*/
